# 答辩指南

## 先记住这一句话

这是一个单 Agent 的命令行 Harness：模型提出工具请求，本地程序掌握历史、权限、执行和终止权，用“模型—工具—反馈”的循环完成编程任务。

若现场紧张，先说这一句，再按“输入、模型、工具、历史、终止”五个词展开。

## 一分钟介绍

“我的项目用 Python 实现，运行时接收一个工作目录和自然语言任务。Agent 把 system prompt 和用户任务发给千问，同时提供四个本地工具的 JSON Schema。模型若返回 Tool Calls，Harness 会解析参数，通过白名单找到本地实现，执行后用 tool_call_id 把结果加入消息历史，再请求下一步；模型不再请求工具而返回文本时结束。为了避免失控，我限制文件路径、命令时间和输出长度，清理子进程敏感环境变量，并设置最多12轮。模型协议、工具执行、循环和 CLI 分成四个模块，离线测试用脚本化假模型保证稳定，最后再用真实千问做端到端验证。”

## 从四个角度理解 Harness

### 它是一套控制流程

模型不拥有本地权限，只能给出建议。Harness 决定建议能否执行、结果怎样记录、何时继续、何时停止。

### 它是一台状态机

状态是 `messages`。一次循环只有两种主要转移：收到 Tool Calls 就执行并继续；收到最终文本就结束。超过最大轮数则进入受控失败状态。

### 它是一层协议翻译

向上理解用户任务和模型的 OpenAI 兼容消息；向下把 `read_file`、`run_command` 等结构化请求翻译成本地 Python 操作，再把结果翻译回 `role=tool` 消息。

### 它是一条信任边界

模型输出和项目文件都不可信。真正的限制必须写在本地代码里，例如路径检查、参数类型、超时和凭据隔离，不能只靠提示词要求模型“注意安全”。

这四种说法描述的是同一个东西，分别强调流程、状态、协议和安全。现场可以根据评委问题切换，不必背同一段定义。

## 高频问题

### 为什么不用 Agent SDK？

题目明确要求核心逻辑自行实现。SDK 会替我处理 Tool Call 解析、循环和历史，难以证明我理解 Harness。本项目只使用通用 HTTP 库 `httpx`，具体协议和控制流都能在源码里找到。

### SDK 到底是什么？

SDK 是某个平台提供的一组开发工具和封装。模型客户端 SDK 往往只是帮忙发 HTTP 请求，题目允许；Agent SDK 还会代管循环、工具和状态，题目禁止。本项目干脆连模型客户端 SDK 也没用，直接实现兼容协议。

### 为什么使用 Tool Calling，不让模型输出一段 JSON？

原生 Tool Calling 有明确的工具名、参数和调用 ID，少一次脆弱的文本提取。它只解决传输格式，本地校验和执行仍由 Harness 完成，因此没有逃避题目核心。

### 对话历史怎样保存？

一次任务内用列表保存 system、user、assistant 和 tool 消息。assistant 的 `tool_calls` 必须原样加入，工具结果再带相同 `tool_call_id` 追加。每轮把完整列表重新发给模型。第一版不做跨任务长期记忆，避免无关复杂度。

### 多个工具调用怎么办？

按照模型返回顺序全部执行，每个结果使用自己的调用 ID。执行完这一批才进入下一次模型请求。测试专门覆盖了同轮两个工具且第二个失败的情况。

### 工具失败为什么不退出？

编程任务中“文件不存在”或“测试失败”通常是有用反馈。Harness 将它包装成 `is_error=true` 的工具消息，模型可以修正路径或代码。API 配置错误、协议损坏和轮数耗尽才会终止本次运行。

### 怎样避免无限循环和费用失控？

模型调用默认最多12轮，HTTP 60秒超时，本地命令30秒超时，输出也有字符上限。达到上限会抛出清楚的受控错误，不再请求模型。

### 怎样限制文件越界？

不能只搜索字符串 `..`。程序把 workspace 与模型路径组合后执行 `Path.resolve()`，再用 `relative_to(workspace)` 验证真实路径仍在工作目录内，因此绝对路径、父目录逃逸和指向外部的现有符号链接都会被拒绝。

### `shell=False` 安全吗？

它避免 Harness 自动让 shell 解释管道、重定向和变量展开，但被启动的 Python、Git 或其他程序仍有当前用户权限。所以我只称它为风险降低，不称为沙箱。不可信项目要放进容器、虚拟机或低权限账号。

### API Key 怎样保护？

Key 只从 `DASHSCOPE_API_KEY` 读取，不写文件、不进 Git、不进视频。模型 URL 必须是 HTTPS。`run_command` 的子进程会移除名称含 Key、Token、Secret 等字样的环境变量，错误信息也不拼接请求头。

### 为什么既有假模型测试，又有真实 API 测试？

它们回答不同问题。脚本化假模型稳定验证历史顺序、多个工具和轮数耗尽，不花钱也不受网络影响；真实测试确认协议、Key、模型能力和完整链路确实兼容。只做其中一种都不够。

### 为什么只有四个工具？

题目功能繁简不限，考察重点是完整 Harness。四个工具已经能查看项目、修改源码和运行测试。工具越多，参数边界和风险也越多；先把最小闭环做透，更容易解释和验证。

### 项目最大的不足是什么？

命令执行没有系统级隔离；上下文始终全量重发，超大项目会遇到上下文成本；`write_file` 是整文件覆盖，没有补丁工具；单 Agent 没有任务规划器。这些都可以继续改，但不影响当前最小闭环成立。

### 如果再给一天，会改什么？

优先把命令放进容器或低权限进程，再增加基于 diff 的编辑工具和上下文裁剪。不会先堆更多模型或多 Agent，因为当前主要短板是隔离与大项目效率。

## 看代码时的路线

1. `src/coding_agent/agent.py`：先看 `Agent.run()`，这是循环主干。
2. `src/coding_agent/model.py`：看请求体和 `_parse_tool_call()`。
3. `src/coding_agent/tools.py`：看注册表、`_resolve_path()` 和 `_run_command()`。
4. `tests/test_agent.py`：看脚本化模型怎样证明 history 正确。
5. `tests/test_live_api.py`：看真实端到端验证。

不要求背每一行。要能沿一条真实任务说明数据怎样从用户走到模型、工具，再回到模型。
