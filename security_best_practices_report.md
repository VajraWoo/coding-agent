# 安全最佳实践审查

## 摘要

审查范围是 Python 3.13 命令行 Harness，不包含 Web 框架。未发现已提交凭据或明文 HTTP 传输等紧急问题。审查新增了两项可验证的安全默认值：本地命令不继承疑似凭据环境变量，模型 Base URL 必须使用 HTTPS。仍有一个无法在本项目内彻底消除的高风险：`run_command` 启动的程序拥有当前 Windows 用户权限；它是编程 Agent 的必要能力，但不是操作系统沙箱。

## 高风险

### SEC-001：命令工具不是操作系统沙箱（未消除，已明确接受）

**影响：若模型受到恶意项目内容诱导或生成危险命令，被启动的程序可能读取工作目录外文件、访问网络或修改当前用户有权访问的资源。**

- 证据：`src/coding_agent/tools.py:204` 使用 `subprocess.run` 启动任意参数数组；`:211` 的 `shell=False` 只避免 Harness 隐式经过 shell，不会降低被启动程序本身的系统权限。
- 已有缓解：文件工具做真实路径边界检查（`:264-268`）；命令固定工作目录、使用参数数组、具有超时和输出上限；答辩笔记不把这些措施称作沙箱。
- 使用要求：只对可信或一次性项目运行；运行前清除无关凭据；更高安全要求应放入容器、虚拟机或低权限系统账号。简单命令白名单并不能解决问题，因为 Python、Git 和测试工具本身也能访问系统。

### SEC-002：子进程继承 API Key 等凭据（已修复）

**影响：模型可要求运行读取环境变量的程序，从而把 API Key 输出到对话或日志。**

- 原因：`subprocess.run` 默认继承父进程全部环境变量。
- 修复：`src/coding_agent/tools.py:212` 显式传入清理后的环境；`:299` 过滤名称含 `KEY`、`TOKEN`、`SECRET`、`PASSWORD`、`CREDENTIAL`、`AUTH` 或 `COOKIE` 的变量。
- 验证：`tests/test_tools.py` 设置伪 Token，确认子进程只能得到 `scrubbed`，同时正常命令测试仍通过。

## 中风险

### SEC-003：不安全服务地址可能泄露 Bearer Key（已修复）

- 原因：API Key 通过 Authorization 头发送（`src/coding_agent/model.py:90`）；原实现接受任意 URL。
- 修复：`src/coding_agent/model.py:59-67` 要求有效 HTTPS URL，且拒绝 URL 内嵌账号密码。
- 剩余边界：HTTPS 只能保护传输，不能证明域名可信。使用者仍应从百炼创建 Key 的弹窗复制 Base URL，不应使用陌生地址。

### SEC-004：项目内容可能包含提示注入（部分缓解）

- 风险：源码、README 或命令输出可能伪装成“给 Agent 的新指令”，诱导模型偏离用户任务。
- 缓解：`src/coding_agent/agent.py:17` 明确把文件与命令输出视为不可信项目数据；工具白名单和路径校验仍在模型之外执行。
- 剩余边界：提示词不能形成强安全隔离。涉及不可信仓库时仍应结合 SEC-001 的系统级隔离。

## 低风险

### SEC-005：依赖版本未做哈希锁定（未处理）

- `pyproject.toml` 对 `httpx` 和 `pytest` 使用兼容版本范围，便于评委安装，但构建不能做到逐字节复现。
- 对本次小型源码评审项目可接受；生产发布可增加锁文件和依赖漏洞扫描。

## 已确认的良好实践

- API Key 只从环境变量读取，异常信息和 CLI 轨迹不打印 Key。
- Git 凭据模式扫描未发现匹配项。
- 文件路径经 `Path.resolve()` 后验证仍属于 workspace。
- HTTP、命令和 Agent 循环均有终止条件。
- 模型产生的工具 JSON 在执行前经过协议和参数验证。
